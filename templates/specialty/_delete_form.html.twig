<form method="post" action="{{ path('app_specialty_delete', {'id': specialty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialty.id) }}">
    <button class="btn">Delete</button>
</form>
